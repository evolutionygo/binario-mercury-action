name: Build YGO Mobile with Docker

on:
  push:
    branches:
      - main  # ✅ Workflow se ejecuta en la rama main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository of the Action
        uses: actions/checkout@v3
        with:
          path: action-repo  # ✅ El Action se ejecuta aquí (donde está el script)

      - name: Debug - Verificar scripts en el Action
        run: |
          echo "📂 Listado de archivos en el repo del Action:"
          ls -l action-repo
          echo "📂 Listado de archivos en action-repo/scripts/"
          ls -l action-repo/scripts || echo "⚠️ El directorio scripts/ no existe en el Action"

      # ✅ Clonamos el REPO PRINCIPAL en la rama SERVER
      - name: Checkout Principal Repository (YGOPRO - MyCard)
        uses: actions/checkout@v3
        with:
          repository: mycard/ygopro
          ref: server  # ✅ Siempre usamos la rama server
          path: base-repo

      # ✅ Clonamos el REPO SECUNDARIO en la rama SERVER
      - name: Checkout Secondary Repository (ygopro-evolution-bin)
        run: |
          echo "🔄 Clonando el repositorio secundario..."
          git clone --branch=server --depth=1 https://github.com/termitaklk/ygopro-evolution-bin merge-repo
          echo "✅ Repositorio secundario clonado en merge-repo/"
          ls -l merge-repo

      - name: Define List of Files to Replace
        run: |
          echo "Definiendo archivos a reemplazar..."
          echo "FILES=ocgcore/ocgcore.cpp ocgcore/ocgcore.h lua/lua_core.cpp" >> $GITHUB_ENV
          echo "✅ Archivos definidos: ocgcore/ocgcore.cpp, ocgcore/ocgcore.h, lua/lua_core.cpp"

      - name: Copy and Replace Selected Files
        run: |
          echo "🔄 Iniciando reemplazo de archivos..."
          for file in $FILES; do
            if [ -f "merge-repo/$file" ]; then
              cp -f "merge-repo/$file" "base-repo/$file"
              echo "✅ Reemplazado: $file"
            else
              echo "⚠️ Archivo no encontrado en el repositorio secundario: $file"
            fi
          done

      - name: Debug - Post Merge File Structure
        run: |
          echo "📂 Estructura de archivos después de la fusión:"
          ls -lR base-repo

      - name: Continue with Build Process (Using Action's Script)
        run: |
          echo "🔄 Iniciando compilación con el script del Action..."
          if [ ! -f action-repo/scripts/build_ygopro.sh ]; then
            echo "❌ ERROR: El script de compilación NO EXISTE en action-repo/scripts/"
            exit 1
          fi
          chmod +x action-repo/scripts/build_ygopro.sh
          action-repo/scripts/build_ygopro.sh
          echo "✅ Compilación finalizada."

      - name: Validate Binary Existence
        run: |
          echo "🔍 Verificando existencia del binario compilado..."
          if [ ! -f base-repo/build/ygopro ]; then
            echo "❌ Binary not found! Error en la compilación."
            exit 1
          fi
          echo "✅ Binario encontrado: base-repo/build/ygopro"

      - name: Upload YGOPRO Binary as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ygopro-binary
          path: base-repo/build/ygopro

      - name: Log - Build Completed
        run: echo "🎉 Proceso completado exitosamente. Binario generado y almacenado como artefacto."





















